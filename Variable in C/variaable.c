// C is a statically type language so we need declare a variable with its type

#include<stdio.h>
int main() {
  int x = 10;
  printf("%d\n", x);
  int y = 15;
  printf("%d\n", y);
  y= 20;
  printf("%d", y);

  return 0;
}